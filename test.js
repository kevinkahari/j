//check all thre object methods 
//Objects method (objects.keys, objects.values, objects.entries)
const car={
    make:'Toyota',
    model:'Camry',
    year:2020,
    color :'red'
};
console.log("model in car:; //return the keys of the object
console.log(Object.values(car)) //return the values of the object
console.log(Object.entries(car)) //return the key value pairs of the object